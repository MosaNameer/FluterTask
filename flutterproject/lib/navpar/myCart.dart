import 'package:flutter/material.dart';

class theCart extends StatelessWidget {
  const theCart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body:  Center(
        child:Text(" cart " ,),
      ),
    );
  }
}
